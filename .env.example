# Copy to .env (gitignored). Strava API app: https://www.strava.com/settings/api
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=

# Locomotion mode (TRF four-gate classifier): copy config/subject_kinematics.local.example.json
# to config/subject_kinematics.local.json (gitignored). See spatial/locomotion_mode.py.
